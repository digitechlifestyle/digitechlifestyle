import { getPillarMetadata, PillarPage } from "@/components/PillarPage";

export const metadata = getPillarMetadata("digital-wealth");

export default function DigitalWealthPage() {
  return <PillarPage slug="digital-wealth" />;
}
